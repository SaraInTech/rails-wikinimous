<div>
  <h2>Wikinimous</h2>
  <ul>
    <% @articles.each do |article| %>
      <li><%= link_to article.title, article_path(article.id)%> <%= link_to "Delete", article_path(article.id), data: {turbo_method: :delete, turbo_confirm: "Confirm"} %></li>
    <% end %>
  </ul>
</div>

<div>
  <%= link_to "Create new article", new_article_path %>
</div>
